%WSD Toolbox
% Version 1.0 30/01/2009
%
%Weighted Spectra functions. 
%  getspec              - Calculate the distribution of eigenvalues. 
%  spec_clust_coef      - Calculate the spectral clustering. 
%  wsd_opt              - Optimise the WSD. 
%  spec_graining        - Spectral coarse graining.     
%  wsd_tune             - tune a generator using WSD
%  wsd                  - Estimate the WSD
%  wsd_grid             - Estimate the WSD in a grid of parameters. 
%  quaternion           - Old. 
%
%Topology generation functions. 
%  genab                - generate an AB graph. 
%  genglp               - generate an GLP graph. 
%  genwax               - waxman graph
%  genwit               - Wealth internet topology graph
%  genpfp               - pfp graph. 
%  geninet              - Inet graph. 
%
%Auxillary functions
%  int_freq             - calc the frequency and classes of intergers
%  mat_freq             - calc the frequency and classes in a matrix. 
%  norm_lap             - calc normalised laplacian from adjacency matrix
%  deg_corr             - calc degree correlations in a graph. 
%  deg_dist             - Estimate the degree distric
%  entropy_rate
%  gacf
%  percentile_contour
